#################
### AUTOSTART ###
#################

# eval $(/usr/bin/gnome-keyring-daemon --start --components=secrets,ssh,gpg)
# export SSH_AUTH_SOCK
# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

# exec-once = $terminal
# exec-once = nm-applet &
# exec-once = waybar & hyprpaper & firefox

exec-once = hypridle
exec-once = hyprpaper
exec-once = sleep 3 && swaync
exec-once = sleep 3 && waybar

exec-once = hyprctl setcursor Bibata-Modern-Ice 24

# Autostart the polkit-gnome authentication agent
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1

# Start the gnome-keyring daemon for secrets, SSH, and GPG management
exec-once = bash -c "eval $(/usr/bin/gnome-keyring-daemon --start --components=secrets,ssh,gpg)"

# # cliphist config for clipboard management
# exec-once = wl-paste --type text --watch cliphist store # Stores only text data
# exec-once = wl-paste --type image --watch cliphist store # Stores only image data

# clipvault for clipboard management
exec-once = wl-paste --watch clipvault store --max-entries 200 --max-entry-age 2d # Store any data, but with additional parameters

# Start battery notification script
# exec-once = bash -c "sleep 5 && ~/.config/scripts/battery-notify.sh"
exec-once = sleep 5 && python3 ~/.config/scripts/battery-notification.py

# Auto-connect Bluetooth device on login
exec-once = sleep 5 && python3 ~/.config/scripts/bluetooth-autoconnect.py

# hyprsunset
exec-once = sleep 5 && hyprsunset --temperature 5800

# syshud for volume and brightness osd
# exec-once = sleep 10 && syshud -p right -o v -m "0 40 0 0"
exec-once = ~/.config/scripts/syshud-pulse-watch.sh

# Auto switch from an empty workspace
exec-once = sleep 5 && python3 ~/.config/scripts/autoswitch.py
